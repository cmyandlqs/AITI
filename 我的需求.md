1. 在前端首页，加上作者sikm的名字；

2. 现在能上网吗，就是我需要让别人再任意一台电脑上都可以使用，怎么做，是不是需要部署？

3. 对于做题的顺序，每次都固定是不是有点僵硬？

---

## 实施方案

### 需求1：首页加作者名

**方案**：在顶部 banner 栏 `NEW 已支持 AITI 交叉解读...` 右侧加入 `by sikm`，与版本号并排。

修改文件：`index.html` 中 `renderWelcome()` 生成的 banner HTML（实际在 `app.js`）。

### 需求2：GitHub Pages 部署

**方案**：将 `aiti/` 目录部署到 GitHub Pages。

步骤：
1. 确认代码已推送到 GitHub 仓库
2. 在仓库 Settings → Pages 中：
   - Source 选 `Deploy from a branch`
   - Branch 选 `main`，文件夹选 `/aiti`
3. 保存后等待 1-2 分钟，即可通过 `https://<username>.github.io/<repo-name>/` 访问

> 注意：如果仓库根目录就是 aiti 项目，可以选根目录 `/`。

### 需求3：题目顺序随机化

**方案**：维度分组打乱 + 组内打乱。

规则：
- 12 个维度（P1, P2, P3, R1, R2, R3, S1, S2, S3, E1, E2, E3）之间的顺序随机打乱
- 同一维度内的 2 道题顺序也随机打乱
- 2 道隐藏题（H1, H2）保持固定在最后
- 随机化在 `startTest()` 中执行，每次开始测试生成新顺序
- 打乱只影响展示顺序，不影响 `state.answers` 的 key（仍然是 `P1-1` 等原始 ID）

修改文件：`src/app.js`
- 新增 `shuffleQuestions()` 函数
- 在 `startTest()` 中调用
- `getAllQuestions()` 改为返回打乱后的列表
- `renderQuestion()` / `selectAnswer()` 无需改动（它们依赖 `state.currentQuestion` 索引和 `q.id`）


## 需求4

将assets\images\方源大头照.jpg以合理的大小和位置显示在我的名称sikm旁边，这是我的头像